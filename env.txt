set(LLVM_DIR $ENV{LLVM_DIR})
set(LLVM_LIB_DIR $ENV{LLVM_LIB_DIR})
set(LLVM_INCLUDE_DIRS $ENV{LLVM_INCLUDE_DIRS})
set(LLVM_LIBS $ENV{LLVM_LIBS})
set(LLVM_DEFINITIONS $ENV{LLVM_DEFINITIONS})
set(LLVM_LD_FLAGS $ENV{LLVM_LD_FLAGS})
set(LLVM $ENV{LLVM})
set(BUILD_HTTP_STATIC $ENV{BUILD_HTTP_STATIC})
set(USE_STATIC_SNDFILE $ENV{USE_STATIC_SNDFILE})
set(CMAKEOPT $ENV{CMAKEOPT})
set(INCLUDE_LLVM $ENV{INCLUDE_LLVM})
set(USE_LLVM_CONFIG $ENV{USE_LLVM_CONFIG})